Technical Skills Survey Project Part 2: Exercise 2 - Meeting with Caesar's Best Friend

1. const alphabet = 'abcdefghijklmnopqrstuvwxyz'


2. let friendsFirst = friend[0]
let findIndex = alphabet.indexOf(friendsFirst)

3. Because indexes begin counting at 0.

4. let newValue = findIndex + shiftValue
let shiftedValue = alphabet[newValue]

5. % (modulous)

6. let findLength = alphabet.length

7. let modIndex = newValue % findLength

8. let message = 'EUXWXV'
let sliceMessage = message.slice(0, 3)